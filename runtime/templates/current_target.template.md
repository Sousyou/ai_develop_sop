<!-- generated_by_ai_develop_sop_runtime_init -->
# Current Target

## Current Goal

Reach a state where the repository can immediately accept the first concrete development task.

## Current Scope

- keep the injected SOP entry usable
- keep the default runtime state usable
- allow the first concrete plan to start without extra manual setup

## Current Validation Target

- host-project bootstrap entry exists
- runtime instance files exist
- AI can enter through the low-token startup path
