# {{HOST_PROJECT_NAME}}

> Initialized with `ai_develop_sop`.

## Overview

This repository has been initialized for AI-assisted development.

If a real project README does not already exist, this placeholder is created by `ai_sop_init.bat` so the project can enter the workflow immediately.

## AI Entry

AI should enter through the host-project `AGENTS.md` generated by `ai_sop_init.bat`.

## Notes

1. This file is only a bootstrap placeholder when the host project had no `README.md`.
2. Product-specific introduction, run instructions, and architecture notes should replace or extend this file in the host project itself.
