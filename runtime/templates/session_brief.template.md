<!-- generated_by_ai_develop_sop_runtime_init -->
# Session Brief

- `refreshed_at`: `{{GENERATED_AT}}`
- `status`: `generated`

## Startup Snapshot

- `project_type`: `common`
- `default_plan_source`: `external_plan_preferred`
- `product_roots_summary`: `fallback:project_root_except_sop_root`
- `current_phase_summary`: `bootstrap_ready`

## Goal Summary

- `long_term_goal_summary`: `Build and evolve the host project {{HOST_PROJECT_NAME}} under AI-assisted development governance.`
- `current_target_summary`: `Reach a state where the repository can immediately accept the first concrete development task.`

## Runtime Summary

- `runtime_flow_delta_summary`: `none`
- `runtime_standards_delta_summary`: `none`

## Default Read Path

1. Read `runtime/entry_state.md`
2. Read this file
3. Read `runtime/project_mount.md` only when exact boundaries are needed
4. Read `ai_project_quickstart.md` when runtime rules are needed
5. Read `ai_project_sop.md` and `sop/*` only when deeper definitions are needed

## Notes

1. This file is a short derived runtime digest.
2. When `runtime/entry_state.md`, `runtime/project_mount.md`, `runtime/project_charter.md`, `runtime/current_target.md`, `ai_runtime_sop.md`, or `ai_runtime_standards.md` changes, the same turn must also update this file.
3. If this file conflicts with authoritative runtime source documents, the source documents win.
