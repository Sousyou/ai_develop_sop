# Project Rules Surface

This directory is the starter seed project-rule surface for copied projects.

## Boundary

- keep starter-seed rule files here for copied projects
- keep starter-owned baseline rules under `../core/rules/*`
- use natural filenames inside this directory
- use `rule-index.md` as the entry
- use `exceptions.md` as the exception registry
