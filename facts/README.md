# Facts

This directory exposes lightweight facts about the source repository at the root.

Use it for:

- high-level repository structure facts
- stable source/product boundary facts
- external orientation docs that should be easy to discover

This directory is not part of the packaged starter surface and should not be copied into target projects by default.

## Current Files

- `repository-model.md`
- `sop-maintenance-rules.md`
